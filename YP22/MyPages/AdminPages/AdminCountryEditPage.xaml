<Page x:Class="YP22.MyPages.AdminPages.AdminCountryEditPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:YP22.MyPages.AdminPages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AdminCountryEditPage">

    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <StackPanel Orientation="Horizontal">
                <TextBlock Margin="5">Наименование:</TextBlock>
                <TextBox TextChanged="TbName_TextChanged" Margin="5" Text="{Binding Name}" Foreground="{Binding Color}" x:Name="TbName" Width="250"></TextBox>
            </StackPanel>
            <Button x:Name="BtnSave" Click="BtnSave_Click" Margin="5" Width="150">Сохранить</Button>
            <Button x:Name="BtnNext" Click="BtnNext_Click" Margin="5" Width="150">Отменить</Button>
        </StackPanel>
    </Grid>
</Page>
